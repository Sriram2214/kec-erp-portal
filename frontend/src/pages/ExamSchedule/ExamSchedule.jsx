import React from 'react'

export default function ExamSchedule() {
  return (
    <div style={{ padding: '4rem', background: 'white', minHeight: '100vh' }}>
      <h1 style={{ color: 'black' }}>EXAM SCHEDULE PAGE IS LIVE</h1>
      <p style={{ color: 'black' }}>If you see this, the routing is working. Now we can add the data back.</p>
    </div>
  )
}
