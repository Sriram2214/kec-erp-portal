{
  "name": "kec-erp-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd .. && if exist public (rd /s /q public) && mkdir public && xcopy /s /e /y frontend\\dist\\* public\\",
    "install-frontend": "cd frontend && npm install"
  },
  "engines": {
    "node": "20.x"
  }
}
