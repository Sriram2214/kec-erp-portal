{
  "name": "kec-erp-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cp -r dist/* ..",
    "install-frontend": "cd frontend && npm install"
  },
  "engines": {
    "node": "20.x"
  }
}
